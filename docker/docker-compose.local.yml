# Local LAN deployment. Usage:
#   docker compose -f docker-compose.yml -f docker-compose.local.yml up -d --build
services:
  caddy:
    volumes:
      - ./Caddyfile.local:/etc/caddy/Caddyfile
      - caddy_data:/data
      - caddy_config:/config
    ports:
      - "8443:8443"
